# 🚀 Деплой Міна Крафт на GitHub Pages

✅ **Білд успішно протестований!**

Preview доступний на: http://localhost:4173/minacraft/

---

# 🚀 Деплой на GitHub Pages

## Автоматичний деплой (рекомендовано)

### Крок 1: Підготовка репозиторію

1. Створіть репозиторій на GitHub (якщо ще не створили)
2. Завантажте код:

```bash
git init
git add .
git commit -m "Initial commit: Міна Крафт гра"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/minacraft.git
git push -u origin main
```

### Крок 2: Налаштування GitHub Pages

1. Перейдіть у **Settings** вашого репозиторію
2. У розділі **Pages**:
   - Source: **GitHub Actions**
   
3. Файл `.github/workflows/deploy.yml` вже створений і автоматично задеплоїть гру

### Крок 3: Оновіть base URL

У файлі `vite.config.js` змініть:

```javascript
base: '/minacraft/', // Замініть на назву ВАШОГО репозиторію
```

Наприклад, якщо ваш репозиторій називається `my-minecraft`:
```javascript
base: '/my-minecraft/',
```

### Крок 4: Запуш зміни

```bash
git add .
git commit -m "Configure GitHub Pages"
git push
```

Деплой відбудеться автоматично! Гра буде доступна за адресою:
`https://ВАШ_USERNAME.github.io/НАЗВА_РЕПО/`

---

## Ручний деплой

Якщо хочете задеплоїти вручну:

### 1. Встановіть залежності:

```bash
npm install
```

### 2. Задеплойте:

```bash
npm run deploy
```

Ця команда:
- Зібере проект (`npm run build`)
- Завантажить білд на гілку `gh-pages`
- Гра буде доступна на GitHub Pages

---

## Налаштування після деплою

### У Settings → Pages:
- Source: **Deploy from a branch**
- Branch: **gh-pages** → **/root**
- Save

Гра буде доступна через кілька хвилин!

---

## Оновлення гри

Після будь-яких змін у коді:

### Автоматичний деплой:
```bash
git add .
git commit -m "Оновлення гри"
git push
```

### Ручний деплой:
```bash
npm run deploy
```

---

## Перевірка білду локально

Перед деплоєм можна перевірити білд:

```bash
npm run build
npm run preview
```

Гра буде доступна на http://localhost:4173/

---

## Troubleshooting

### Проблема: Білі екран після деплою
- Перевірте `base` у `vite.config.js` - він має відповідати назві репозиторію

### Проблема: 404 помилка
- Переконайтеся що GitHub Pages включений
- Перевірте що обрано правильну гілку (gh-pages)

### Проблема: Гра не оновлюється
- Очистіть кеш браузера (Ctrl+Shift+R або Cmd+Shift+R)
- Почекайте 5-10 хвилин після пушу

---

**Готово! Тепер ваша гра доступна онлайн! 🎮**

